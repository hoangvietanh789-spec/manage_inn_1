from setuptools import setup, find_packages

setup(
    name="manage",
    version="0.1.0",
    description="A simple example package called manage",
    author="Your Name",
    packages=find_packages(),
    python_requires=">=3.6",
)
